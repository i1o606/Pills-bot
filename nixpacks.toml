[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -c 'import nltk; nltk.download('punkt')'"]

[start]
cmd = "python main.py"
